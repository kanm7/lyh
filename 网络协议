Http与Https
都是数据在互联网上传输所要遵循的协议。只不过前者是明文传输，不安全，后者是加密传输，保证了安全。
https需要从 CA（证书颁发机构）申请 SSL/TLS 证书。
虽然现在大家习惯上仍统称为“SSL”，但实际上现代互联网几乎都在运行 TLS 协议。它的核心任务是确保你的浏览器和服务器之间的对话是私密的（不被窃听）、完整的（不被篡改）且真实的（对方身份可靠）。
握手过程 (The Handshake) 🤝：揭秘浏览器和服务器在第一次见面时，是如何通过交换“暗号”来建立信任的。
你可以把 SSL/TLS 握手想象成浏览器（你）和服务器（网站）在正式开始“悄悄话”之前，先进行的一场“见面礼”。它的目的很简单：确认身份并商量暗号。
握手的三个关键动作 ⚡
1. 打招呼 (Client/Server Hello)：
你的浏览器先敲敲门说：“你好，我想安全地访问你，我这里有几种加密方法，你看哪种合适？”服务器会挑出一种大家都支持的方法回复说：“好的，就按这种来。”
2. 看证件 (Certificate)：
服务器为了证明自己不是冒牌货，会发来一张数字证书 📜（就像身份证）。浏览器会去查验这张证：是不是真的？有没有过期？
3. 对暗号 (Key Exchange)：
身份确认后，双方会通过一种神奇的数学方式，商量出一个临时暗号。这个暗号只有你们两个知道，接下来的所有数据都会用这个暗号“上锁”。


在网络世界里，它们的执行顺序是极其严格的，就像盖房子必须先打地基：
1. 第一步：TCP 三次握手（建立物理通道）。
2. 第二步：SSL/TLS 握手（在通道里加密）。
3. 第三步：发送真正的 HTTP 数据（比如网页内容）。

加密是 SSL/TLS 的核心。为了兼顾安全和速度，它并没有只用一种加密方式，而是玩了一个“组合拳”。
我们可以把加密想象成两种不同类型的保险箱：
1. 对称加密 (Symmetric Encryption) 🔑
• 特点：加密和解密使用的是同一把钥匙。
• 优点：计算简单，处理数据的速度非常快。
• 缺点：钥匙的传输是个难题。如果你在互联网上把钥匙发给对方，一旦被黑客截获，后续所有的加密通讯就等同于裸奔。
2. 非对称加密 (Asymmetric Encryption) 🔓🔐
• 特点：有一对钥匙，公钥（公开给所有人）和私钥（自己严密保管）。
• 神奇之处：用公钥加密后的数据，只有对应的私钥能解开；反之亦然。
• 优点：非常安全，因为私钥永远不需要在网络上传输。
• 缺点：计算极其复杂，速度比对称加密慢得多。

为了让网络既安全又快速，SSL/TLS 采用了混合加密机制：
1. 握手阶段：使用非对称加密。服务器把公钥发给浏览器，浏览器生成一把临时的“对称钥匙”，用公钥锁住后传给服务器。这样，只有拥有私钥的服务器能打开它。
2. 传输阶段：一旦双方都安全地拿到了这把“对称钥匙”，后续所有的网页内容、图片等大数据量传输，就全部改用对称加密。
