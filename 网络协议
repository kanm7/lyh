### Http与Https

都是数据在互联网上传输所要遵循的协议。只不过前者是明文传输，不安全，后者是加密传输，保证了安全。
https需要从 CA（证书颁发机构）申请 SSL/TLS 证书。
虽然现在大家习惯上仍统称为“SSL”，但实际上现代互联网几乎都在运行 TLS 协议。它的核心任务是确保你的浏览器和服务器之间的对话是私密的（不被窃听）、完整的（不被篡改）且真实的（对方身份可靠）。

#### 握手过程 (The Handshake) 🤝：揭秘浏览器和服务器在第一次见面时，是如何通过交换“暗号”来建立信任的。

你可以把 SSL/TLS 握手想象成浏览器（你）和服务器（网站）在正式开始“悄悄话”之前，先进行的一场“见面礼”。它的目的很简单：确认身份并商量暗号。
握手的三个关键动作 ⚡

1. 打招呼 (Client/Server Hello)：
你的浏览器先敲敲门说：“你好，我想安全地访问你，我这里有几种加密方法，你看哪种合适？”服务器会挑出一种大家都支持的方法回复说：“好的，就按这种来。”
2. 看证件 (Certificate)：
服务器为了证明自己不是冒牌货，会发来一张数字证书 📜（就像身份证）。浏览器会去查验这张证：是不是真的？有没有过期？
3. 对暗号 (Key Exchange)：
身份确认后，双方会通过一种神奇的数学方式，商量出一个临时暗号。这个暗号只有你们两个知道，接下来的所有数据都会用这个暗号“上锁”。

#### 在网络世界里，它们的执行顺序是极其严格的，就像盖房子必须先打地基：

1. 第一步：TCP 三次握手（建立物理通道）。
2. 第二步：SSL/TLS 握手（在通道里加密）。
3. 第三步：发送真正的 HTTP 数据（比如网页内容）。

#### 加密是 SSL/TLS 的核心。为了兼顾安全和速度，它并没有只用一种加密方式，而是玩了一个“组合拳”。

我们可以把加密想象成两种不同类型的保险箱：

1. 对称加密 (Symmetric Encryption) 🔑
• 特点：加密和解密使用的是同一把钥匙。
• 优点：计算简单，处理数据的速度非常快。
• 缺点：钥匙的传输是个难题。如果你在互联网上把钥匙发给对方，一旦被黑客截获，后续所有的加密通讯就等同于裸奔。
2. 非对称加密 (Asymmetric Encryption) 🔓🔐
• 特点：有一对钥匙，公钥（公开给所有人）和私钥（自己严密保管）。
• 神奇之处：用公钥加密后的数据，只有对应的私钥能解开；反之亦然。
• 优点：非常安全，因为私钥永远不需要在网络上传输。
• 缺点：计算极其复杂，速度比对称加密慢得多。

#### 为了让网络既安全又快速，SSL/TLS 采用了混合加密机制：

1. 握手阶段：使用非对称加密。服务器把公钥发给浏览器，浏览器生成一把临时的“对称钥匙”，用公钥锁住后传给服务器。这样，只有拥有私钥的服务器能打开它。
2. 传输阶段：一旦双方都安全地拿到了这把“对称钥匙”，后续所有的网页内容、图片等大数据量传输，就全部改用对称加密。

### TCP

#### 三次握手

浏览器和服务器在正式发送数据之前，需要先建立连接。

### 握手的三个步骤 ⚡

1. **第一步：客户端发送 (SYN)** 🙋‍♂️ 客户端发信号说：“你好，我想和你建立连接（**SYN**=1）。你能听到我说话吗？” *这时候服务器知道：客户端发包正常，自己收包正常。*
2. **第二步：服务器回应 (SYN + ACK)** 🤖 服务器收到后回复：“我听到你说话了（**ACK**=1），我也想和你建立连接（**SYN**=1）。你能听到我说话吗？” *这时候客户端知道：双方的收发功能全部正常！*
3. **第三步：客户端确认 (ACK)** ✅ 客户端最后回复：“我也听到你说话了（**ACK**=1），我们开始聊吧！” *这时候服务器也终于确认：客户端能听到我说话。*

**SYN (Synchronize)**：同步序列号。它是请求建立连接的信号。

**ACK (Acknowledge)**：确认号。它是对收到信号的“收到”回执。

### 面试话术：一句话概括 TCP 三次握手

> TCP 三次握手是建立**可靠连接**的过程。首先客户端发送 **SYN** 报文请求连接；随后服务端回应 **SYN+ACK** 报文，表示同意连接并同步自己的初始序列号；最后客户端再发送 **ACK** 报文确认收到。这三次交互的核心目的是：**同步双方的初始序列号 (ISN)** 并**确认双方都具备完整的发送和接收能力**。

#### 两次握手为什么不行？最少三次？

三次握手确保了服务端只有在确认客户端真的收到了自己的回复后，才正式分配资源。

#### 四次挥手

我们可以把这想象成两个人在通电话结束时的场景：

### 四次挥手的过程 ⚡

1. **第一步：客户端发起断开 (FIN)** 🙋‍♂️ 客户端说：“我这边想说的话都说完了，我想挂电话了（发送 **FIN** 报文）。”
2. **第二步：服务端确认 (ACK)** 🤖 服务端回答：“我知道你想挂了（发送 **ACK**），但我这边可能还有点话没说完，你稍等我一下。” *这时候，连接处于“半关闭”状态。客户端不再发数据，但服务端如果还有数据，可以继续传。*
3. **第三步：服务端发起断开 (FIN)** 🤖 过了一会儿，服务端也说完了：“好了，我这边也说完了，我也准备挂了（发送 **FIN**）。”
4. **第四步：客户端最后确认 (ACK)** ✅ 客户端回复：“收到，那再见吧（发送 **ACK**）。”

### 核心名词解释 🔍

- **FIN (Finish)**：结束标志位。用来告诉对方，本端的数据已经发送完毕，请求关闭连接。
- **半关闭 (Half-Close)**：指 TCP 连接的一端已经关闭了发送通道，但仍然可以接收来自另一端的数据。

#### 总结概括

“**TCP 四次挥手**是释放连接的过程。首先，主动方发送 **FIN** 报文表示不再发送数据；被动方回复 **ACK** 确认，此时连接进入**半关闭状态**，被动方仍可继续发送未传完的数据。当被动方也发送完数据后，会发送自己的 **FIN** 报文。最后，主动方回传 **ACK** 确认，并在等待 **2MSL**（最大报文生存时间）后彻底关闭连接，以确保最后一个 ACK 能够送达，并防止旧的连接请求干扰后续的新连接。”

#### TCP三次握手最后一个Ack包没有送到就开始传输数据包，会发生什么？

服务端不会接收数据包，并且会返回给客户端RST包，也就是异常包

### HTTP 1.0：短连接时代 🕒

- **默认短连接**：每次请求资源（如一张图片、一个 JS 文件）都要重新经历一次 TCP 三次握手，请求完立刻断开。
- **手动长连接**：如果想保持连接，必须手动在 Header 中设置 `Connection: keep-alive`。
- **缺点**：频繁建立和关闭连接，浪费了大量的时间和服务器资源。

### HTTP 1.1：长连接与文本时代 📝

- **默认长连接**：这是 1.1 的重大改进，连接建立后可以多次复用，减少了握手的开销。
- **基于文本格式**：所有的 Header 和数据都是以普通的文本形式传输的。
- **瓶颈（队头阻塞）**：虽然连接保持了，但同一个连接里请求必须排队。如果第一个请求慢了，后面的都得等着。

### HTTP 2.0：二进制与效率时代 🚀

这是目前主流的版本，做了很多“破格”的优化：

- **二进制传输**：不再像以前那样传文本，而是把数据切分成二进制帧。机器读二进制比读文本快得多，也更安全。
- **多路复用 (Multiplexing)**：这是最核心的进步。它允许在**同一个连接**里同时发送多个请求和响应，互不干扰，彻底解决了排队等待的问题。
- **Header 压缩**：以前每个请求都要带上重复的、巨大的 Header 信息，2.0 通过压缩技术把这部分体积减到了最小。
- **服务端推送 (Server Push)**：服务器可以“预判”你的需求。比如你请求了 `index.html`，服务器知道你肯定还需要 `style.css`，就会主动把它推送到你的缓存里，不用等你再开口要。

#### HTTP1.0 和 HTTP1.1 的区别

HTTP1.0 默认短连接，可以长连接，但是需要 设置 header connection: keepAlive 

HTTP1.1 默认长连接

#### HTTP1.0，HTTP1.1 和 HTTP2.0 的区别

**HTTP2.0 支持二进制传输数据**，更加安全快捷，而 HTTP1.0，HTTP1.1 支持文本

**HTTP2.0 实现多路复用**，更加快捷

**HTTP2.0 压缩 header**

**HTTP2.0 支持服务“主动”给客户端缓存发送数据**
